// This is an example file.
// Create a new file named `config.js` in this same `js` directory.
// Copy the contents of this file into `config.js` and fill in your actual Firebase project keys.
// The `config.js` file is already in .gitignore, so your keys will be safe.

export const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID",
    measurementId: "YOUR_MEASUREMENT_ID"
}; 